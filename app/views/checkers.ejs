<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Checkers</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
  </head>
  <body>
    <div>
      <table>
        <% for(var i = 1; i < 9; i++) { %>
          <tr id=row<%= i %>>
            <% for(var j = 1; j < 9; j++) { %>
              <td>test</td>
            <% } %>
          </tr>
        <% } %>
      </table>
    </div>
  </body>
</html>

